function clickMenu_SampleTest(menuObject){
	Dialog.open({
		div : '#openApiUrlMapping', 
		url : baseUrl+"api/openApiUrl001?dialogName=openApiUrlMapping",
		title : 'OpenApi Url Mapping', 
		width : 800, 
		height : 520, 
		modal : true
	});
}