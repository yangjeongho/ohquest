
/* 리소스 제조사정보조회 */
function clickMenu_res001(menuObject){
		Dialog.open({
			div : '#dialogContent', 
			url : baseUrl+"res/res001"+"?dialogName=dialogContent", 
			title : 'Inquiry Maker', 
			width : 1000, 
			height : 720, 
			modal : true
		});
}

/* 리소스 모델조회 */
function clickMenu_res002(menuObject){
		Dialog.open({
			div : '#dialogContent', 
			url : baseUrl+"res/res002"+"?dialogName=dialogContent", 
			title : 'Inquiry Model', 
			width : 1000, 
			height : 700, 
			modal : true
		});
}

/* 리소스 모델분류조회 */
function clickMenu_res003(menuObject){
	Dialog.open({
		div : '#dialogContent', 
		url : baseUrl+"res/res003"+"?dialogName=dialogContent", 
		title : 'Inquiry Model Class', 
		width : 1000, 
		height : 700, 
		modal : true
	});
}

/* 리소스 로딩 */
function clickMenu_res004(menuObject){
	Dialog.open({
		div : '#dialogContent', 
		url : baseUrl+"res/res004"+"?dialogName=dialogContent", 
		title : 'Resource Registration', 
		width : 1176, 
		height : 780, 
		modal : true
	});
}

/* 리소스 분배 관리 */
function clickMenu_res005(menuObject){
	Dialog.open({
		div : '#dialogContent', 
		url : baseUrl+"res/res005"+"?dialogName=dialogContent", 
		title : 'Resource Distribute Management', 
		width : 1000, 
		height : 800, 
		modal : true
	});
}

/* 리소스 마스터 관리 */
function clickMenu_res006(menuObject){
	Dialog.open({
		div : '#dialogContent', 
		url : baseUrl+"res/resMaster"+"?dialogName=dialogContent", 
		title : 'Resource Master Management', 
		width : 1000, 
		height : 620, 
		modal : true
	});
}

/* 리소스 일별재고조회 */
function clickMenu_res007(menuObject){
	Dialog.open({
		div : '#dialogContent', 
		url : baseUrl+"res/res007"+"?dialogName=dialogContent", 
		title : 'Resource Daily Stock Management', 
		width : 1000, 
		height : 620, 
		modal : true
	});
}

/* 리소스 월별재고조회 */
function clickMenu_res008(menuObject){
	Dialog.open({
		div : '#dialogContent', 
		url : baseUrl+"res/res008"+"?dialogName=dialogContent", 
		title : 'Resource Monthly Stock Management', 
		width : 1000, 
		height : 590, 
		modal : true
	});
}

/* 리소스 할당 관리 */
function clickMenu_res009(menuObject){
	Dialog.open({
		div : '#dialogContent', 
		url : baseUrl+"res/res009"+"?dialogName=dialogContent", 
		title : 'Resource Assign Management', 
		width : 1000, 
		height : 800, 
		modal : true
	});
}

/* IMSI Upload */
function clickMenu_res010(menuObject){
	Dialog.open({
		div : '#dialogContent', 
		url : baseUrl+"res/res010"+"?dialogName=dialogContent", 
		title : 'IMSI Upload', 
		width : 1176, 
		height : 780, 
		modal : true
	});
}

/* IMSI 할당 */
function clickMenu_res011(menuObject){
	Dialog.open({
		div : '#dialogContent', 
		url : baseUrl+"res/res011"+"?dialogName=dialogContent", 
		title : 'IMSI Allocation', 
		width : 900, 
		height : 780, 
		modal : true
	});
}

/* IMSI Changer */
function clickMenu_res012(menuObject){
	Dialog.open({
		div : '#dialogContent', 
		url : baseUrl+"res/res012"+"?dialogName=dialogContent", 
		title : 'Change Status of IMSI', 
		width : 900, 
		height : 600, 
		modal : true
	});
}

/* Number Distribute */
function clickMenu_res013(menuObject){
	Dialog.open({
		div : '#dialogContent', 
		url : baseUrl+"res/res013"+"?dialogName=dialogContent", 
		title : 'Number Distribution', 
		width : 1000, 
		height : 800, 
		modal : true
	});
}

/* number resource */
function clickMenu_res020(menuObject){
	Dialog.open({
		div : '#dialogContent', 
		url : baseUrl+"res/res020?dialogName=dialogContent", 
		title : 'Number Resource', 
		width : 1000, 
		height : 600, 
		modal : true
	});
}


/* number pattern */
function clickMenu_res021(menuObject){
	Dialog.open({
		div : '#dialogContent', 
		url : baseUrl+"res/res021?dialogName=dialogContent", 
		title : 'Number Grade Management', 
		width : 850,
		height : 670,
		modal : true
	});
}


/* Model Sale Policy */
function clickMenu_res014(menuObject){
	Dialog.open({
		div : '#dialogContent', 
		url : baseUrl+"res/res014?dialogName=dialogContent", 
		title : 'Model Sale / Lease Policy', 
		width : 1000, 
		height : 720, 
		modal : true
	});
}


/* number Pattern Pool resource */
function clickMenu_res022(menuObject){
	Dialog.open({
		div : '#dialogContent', 
		url : baseUrl+"res/res022?dialogName=dialogContent", 
		title : 'Number Pattern Pool', 
		width : 1000, 
		height : 600, 
		modal : true
	});
}


function clickMenu_res023(menuObject){
	Dialog.open({
		div : '#dialogContent', 
		url : baseUrl+"res/res023?dialogName=dialogContent", 
		title : 'Number Aging Period', 
		width : 650, 
		height : 650, 
		modal : true
	});
}
